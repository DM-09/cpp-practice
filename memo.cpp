/* 헤더 파일 */
#include <string>
#include <vector>
#include <algorithm>

/* std 단축 명령어 */
using namespace std;

/*  
[베열 정렬]
#include <algorithm>
sort(array.begin(), array.end()); // =  list.sort() (In python)
*/


/*  
[베열 정렬]
#include <algorithm>
count(array.begin(), array.end(), i); // i는 찾고 싶은거
*/
