/* 헤더 파일 */
#include <string>
#include <vector>
#include <algorithm>

/* std 단축 명령어 */
using namespace std;

/*  
[베열 정렬]
#include <algorithm>
sort(array.begin(), array.end()); // =  list.sort() (In python)
*/
